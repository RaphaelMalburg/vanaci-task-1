# Example environment variables for the chat application

# URL do webhook público do n8n (entrada)
N8N_WEBHOOK_URL=https://seu-n8n.com/webhook/xxxxx

# Segredo para o proxy Next -> n8n (opcional; você valida no n8n)
N8N_BEARER=vanaci-secret-key

# Segredo para validar o webhook vindo do n8n -> Next
APP_WEBHOOK_SECRET=vanaci-secret-key

# IPs permitidos para webhooks (opcional, separados por vírgula)
# Deixe vazio para permitir todos os IPs
ALLOWED_IPS=
